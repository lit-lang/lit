module Lit
  KEYWORDS = {
    and:    TokenType::AND,
    else:   TokenType::ELSE,
    false:  TokenType::FALSE,
    fn:     TokenType::FN,
    if:     TokenType::IF,
    let:    TokenType::LET,
    nil:    TokenType::NIL,
    or:     TokenType::OR,
    print:  TokenType::PRINT,
    puts:   TokenType::PUTS,
    return: TokenType::RETURN,
    true:   TokenType::TRUE,
    while:  TokenType::WHILE,
  }
end
