let before = clock();
fn sum { |n|
  let i = 0;
  let total = 0;
  while i < n {
    total = total + i;
    i = i + 1;
  }
  return total;
}

sum(10_000_000);
let after = clock();
print "Time taken: ";
print (after - before);
println "s";
# Time taken: 2.559272050857544s
# on release build
