# False and nil are false.
println(false and "bad") # expect: false
debug(nil and "bad") # expect: nil

# Everything else is true.
println(true and "ok") # expect: ok
println(0 and "ok") # expect: ok
println("" and "ok") # expect: ok
