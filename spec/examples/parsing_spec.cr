require "../spec_helper"

describe "Examples" do
  describe "parsing" do
    # example_files = Dir.glob("examples/*.lit").sort.first(1)

    # example_files.each do |file|
    #   it "parses #{file} without errors" do
    #     error = nil

    #     begin
    #       Lit::Parser.parse(Lit::Scanner.scan(File.read(file)))
    #     rescue e
    #       error = e
    #     end

    #     error.should be_nil
    #   end
    # end
  end
end
