let double = fn { it * 2 }

[1, 2, 3].map(double).each { println(it) } # 2 4 6

let users = [{name: "John", age: 30}, {name: "Jane", age: 28}]

users.each { println("{it.name} is {it.age}") }
users.each { println("{its.name} is {its.age} years old") } # does same thing, but reads better
